import 'package:flutter/material.dart';

class add_todo_dialog extends StatelessWidget {
  const add_todo_dialog({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('add_todo_dialog is working'),
      ),
    );
  }
}